export enum Status {
  NORMAL = 'NORMAL',
  DELETED = 'DELETED',
}

export enum QuestionType {
  RADIO = 'RADIO',
  CHECKBOX = 'CHECKBOX',
  SHORTFORM = 'SHORTFORM',
}

export enum SelectionType {
  LETTER = 'LETTER',
  IMAGE = 'IMAGE',
}
